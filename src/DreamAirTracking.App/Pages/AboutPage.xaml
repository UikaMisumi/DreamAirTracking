<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DreamAirTracking.App.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DreamAirTracking.App.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="About" />
        <Grid Grid.Row="1" MaxWidth="760" HorizontalAlignment="Left" ColumnSpacing="24" RowSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Width="160"
                Height="160"
                CornerRadius="80"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
                <Image Source="/Assets/DeveloperAvatar.png" Stretch="UniformToFill" />
            </Border>

            <StackPanel Grid.Column="1" Spacing="8" VerticalAlignment="Center">
                <TextBlock FontSize="28" FontWeight="SemiBold" Text="DreamAirTracking" />
                <TextBlock FontSize="18" Text="Developer: Sumirui / UikaMisumi" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Dream Air eye tracking calibration and VRCFT eye output." />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Spacing="12">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Headset" />
                <ComboBox Width="260" SelectedIndex="0" IsEnabled="False">
                    <ComboBoxItem Content="Dream Air" />
                </ComboBox>
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Text="BrokenEye -> DreamAirTracking -> VRCFT eye slot. Face and expression slots stay with SR or another face module." />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
